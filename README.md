# Screenshot Downloader (App Store + Google Play)

Python-инструмент для автоматического скачивания **скриншотов** и **видео-превью** из **Apple App Store** и **Google Play** в максимальном качестве и с правильной локализацией.

## 🚀 Основные возможности

* **Два магазина:** App Store и Google Play в одном инструменте.
* **Видео-превью (App Store):** скачиваются HLS-плейлисты `.m3u8` и склеиваются в `.mp4` через `ffmpeg` — для iPhone и iPad одновременно.
* **Корректная локализация:** скриншоты и видео подгружаются на языке выбранной страны (vn → вьетнамский, ru → русский, jp → японский и т.д.) — не английский фолбэк.
* **Принимает любой формат входа:**
  * App Store: `1534886813`, `id1534886813`, `https://apps.apple.com/us/app/.../id1534886813` (с любыми query-параметрами и кириллицей в slug).
  * Google Play: `com.example.app`, `https://play.google.com/store/apps/details?id=com.example.app&hl=en`.
* **Гибридная загрузка (App Store):** iTunes API для скорости + парсинг HTML как fallback (для приложений вроде *Lingokids* и *MathHero*, где API возвращает пустой список).
* **Максимальное качество:** App Store — подстановка `1284x2778bb.jpg`; Google Play — суффикс `=w0` (оригинальный размер).
* **Строгий порядок:** скриншоты сохраняются в той же последовательности, как в магазине.
* **Поддержка регионов:** любая страна (US, RU, KZ, VN, GB, DE, JP, BR и т.д.).
* **Умная организация:** папки с указанием локали и магазина (`MathHero_Maths_Games_for_Kids_vn_appstore`, `Duolingo_ru_gplay`).
* **Умная фильтрация (App Store):** отсеивает иконки, плейсхолдеры, баннеры, видео-обложки. Скачиваются только реальные скриншоты.
* **Дедупликация по базовому пути:** одна картинка в десятках размеров из srcset схлопывается в одну запись.
* **Авточистка папки:** перед каждым прогоном старые `screen_*` и `preview_*` удаляются — никаких смешений между запусками.
* **Диагностический лог для видео:** видно, сколько ссылок нашлось на iPhone- и iPad-странице отдельно.

## 📋 Требования

* Python 3.10+
* Библиотеки: `requests`, `google-play-scraper`
* **ffmpeg** (системная утилита) — для скачивания видео-превью App Store. Без него скриншоты скачиваются как обычно, видео пропускаются с предупреждением.
  * macOS: `brew install ffmpeg`
  * Ubuntu/Debian: `apt install ffmpeg`
  * Windows: [ffmpeg.org](https://ffmpeg.org/download.html)

## 🛠 Установка

```bash
git clone <repo-url>
cd app-store-grabber
pip install -r requirements.txt
```

## 💻 Использование

```bash
python main.py
```

**Интерактивный режим:**
1. Введите ID, название, package name или URL приложения
2. Выберите магазин: `a` — App Store, `g` — Google Play
3. Введите код страны (по умолчанию `us`)

**Пример — App Store, вьетнамская локаль с видео-превью:**
```
=== Screenshot Downloader v8.0 (App Store + Google Play) ===

1. Введите ID или название (exit): https://apps.apple.com/vn/app/mathhero-maths-games-for-kids/id1534886813
2. Магазин — (a)pp store / (g)oogle play [a]: a
3. Страна (us, ru, kz) [us]: vn

Цель: MathHero_Maths_Games_for_Kids (VN) -> Папка: MathHero_Maths_Games_for_Kids_vn_appstore
   [i] API пуст. Перехожу к сканированию сайта...
--- Найдено 9 ссылок. Начинаю загрузку в 'MathHero_Maths_Games_for_Kids_vn_appstore'...
    [+] screen_1.jpg (97 KB)
    ...
--- Готово. Скачано файлов: 9

   [i] iPhone: уникальных видео-ссылок 1
   [i] iPad: уникальных видео-ссылок 1
--- Видео-превью: найдено 2 шт. Скачиваю через ffmpeg...
    [+] preview_1.mp4 (10116 KB)
    [+] preview_2.mp4 (35840 KB)
--- Видео скачано: 2
```

**Пример — Google Play, русская локаль:**
```
1. Введите ID или название (exit): https://play.google.com/store/apps/details?id=com.duolingo
2. Магазин — (a)pp store / (g)oogle play [a]: g
3. Страна (us, ru, kz) [us]: ru

Цель: Duolingo_-_языки_и_шахматы (RU) -> Папка: Duolingo_-_языки_и_шахматы_ru_gplay
   [i] Найдено скриншотов: 6 шт.
--- Найдено 6 ссылок. Начинаю загрузку в 'Duolingo_-_языки_и_шахматы_ru_gplay'...
    [+] screen_1.png (84 KB)
    ...
--- Готово. Скачано файлов: 6
```

## ⚙️ Как это работает

### App Store

1. **Распознавание входа.** Если на входе URL или строка с `idXXXXXXXX` — извлекается чистый ID и идёт точечный запрос в iTunes Lookup API. Иначе — поисковый запрос по тексту.
2. **iTunes API.** `itunes.apple.com/lookup?id=...&country=<cc>`. Возвращает метаданные приложения и (иногда) ссылки на скриншоты. На основе ответа формируется имя папки и базовый URL страницы (`trackViewUrl`).
3. **Локализация:** к URL страницы приклеивается `?l=<язык>` по таблице `COUNTRY_LANG` (например, `vn` → `vi`, `de` → `de-DE`, `br` → `pt-BR`). Без этого Apple часто отдаёт английский фолбэк, даже если у приложения есть локализованные ассеты.
4. **Скриншоты:**
   * Если в API есть `screenshotUrls` — берутся они напрямую.
   * Если нет (типичный кейс для новых приложений) — парсится HTML страницы с `?l=<язык>`, ссылки извлекаются регуляркой по CDN `mzstatic.com/image/thumb/...`, фильтруются от иконок/баннеров/плейсхолдеров и дедуплицируются по базовому пути.
5. **Хак качества:** в URL подставляется размер `1284x2778bb.jpg`. Если суффикса нет — добавляется.
6. **Видео-превью:** парсятся обе версии страницы продукта (по умолчанию для iPhone и `?platform=ipad` для iPad), извлекаются `.m3u8` плейлисты, дедуплицируются. Каждый плейлист скачивается через `ffmpeg -c copy` (без перекодирования) в `.mp4`. По каждому варианту страницы пишется лог, сколько уникальных ссылок нашлось.

### Google Play

1. **Распознавание входа.** Из строки извлекается package name: либо параметр `?id=...` из URL, либо сама строка (если она имеет вид `com.example.app`). Иначе — текстовый поиск.
2. **Локализация:** язык вычисляется по стране через ту же таблицу `COUNTRY_LANG`, но в коротком виде (`en-US` → `en`, `pt-BR` → `pt`). Передаётся параметром `lang=` во все вызовы `gplay_app` и `gplay_search`.
3. **Скриншоты:** возвращаются библиотекой `google-play-scraper` в поле `screenshots`. К URL дописывается суффикс `=w0` для получения оригинального размера.
4. **Fallback при `appId=None`:** если библиотека не вернула ID, парсится HTML страницы поиска Google Play с `&hl=<lang>&gl=<country>`.

## 🌍 Поддержка локализации

В `COUNTRY_LANG` уже добавлены основные страны: us, gb, ca, au, ru, kz, by, ua, vn, jp, kr, cn, tw, th, id, de, at, ch, fr, be, es, it, pt, br, mx, ar, cl, co, tr, pl, nl, se, no, dk, fi, cz, sk, hu, ro, gr, il, sa, ae, eg.

Если нужна другая страна — добавь строку в словарь `COUNTRY_LANG` в начале `main.py`. Для неизвестных стран по умолчанию используется `en`.

## 📂 Структура проекта
```
.
├── main.py             # Основной скрипт
├── requirements.txt    # Зависимости (Python)
├── .gitignore          # Правила игнорирования файлов
└── README.md           # Документация
```

## 📝 Лицензия

Проект распространяется под лицензией MIT.
