# Screenshot Downloader (App Store + Google Play)

Python-инструмент для автоматического скачивания скриншотов из **Apple App Store** и **Google Play** в максимальном качестве.

## 🚀 Основные возможности

* **Два магазина:** Поддержка App Store и Google Play в одном инструменте.
* **Гибридная загрузка (App Store):** iTunes API для скорости + парсинг HTML как fallback (для приложений вроде *Lingokids*, где API возвращает пустой список).
* **Google Play:** Поиск по названию или package name (`com.example.app`) через `google-play-scraper`.
* **Максимальное качество:** App Store — подстановка `1284x2778bb.jpg`; Google Play — суффикс `=w0` (оригинальный размер).
* **Строгий порядок:** Скриншоты сохраняются в той же последовательности, как в магазине.
* **Поддержка регионов:** Любая страна (US, RU, KZ, GB, DE и т.д.).
* **Умная организация:** Папки с указанием локали и магазина (`Telegram_us_appstore`, `Telegram_us_gplay`).
* **Умная фильтрация (App Store):** Отсеивает иконки, плейсхолдеры, баннеры, видео-превью. Скачиваются только реальные скриншоты.
* **Дедупликация по базовому пути:** Одна картинка в десятках размеров из srcset схлопывается в одну запись.

## 📋 Требования

* Python 3.10+
* Библиотеки: `requests`, `google-play-scraper`

## 🛠 Установка

```bash
git clone <repo-url>
cd app-store-grabber
pip install -r requirements.txt
```

## 💻 Использование

```bash
python main.py
```

**Интерактивный режим:**
1. Введите ID/название приложения (или package name для Google Play)
2. Выберите магазин: `a` — App Store, `g` — Google Play
3. Введите код страны (по умолчанию `us`)

**Пример — App Store:**
```
=== Screenshot Downloader v8.0 (App Store + Google Play) ===

1. Введите ID или название (exit): Lingokids
2. Магазин — (a)pp store / (g)oogle play [a]: a
3. Страна (us, ru, kz) [us]: us

Цель: Lingokids_-_Play_and_Learn (US) -> Папка: Lingokids_-_Play_and_Learn_us_appstore
   [i] API пуст. Перехожу к сканированию сайта...
--- Найдено 16 ссылок. Начинаю загрузку в 'Lingokids_-_Play_and_Learn_us_appstore'...
    [+] screen_1.jpg (305 KB)
    ...
--- Готово. Скачано файлов: 16
```

**Пример — Google Play:**
```
1. Введите ID или название (exit): Lingokids
2. Магазин — (a)pp store / (g)oogle play [a]: g
3. Страна (us, ru, kz) [us]: us

Цель: Lingokids_-_Play_and_Learn (US) -> Папка: Lingokids_-_Play_and_Learn_us_gplay
   [i] Найдено скриншотов: 24 шт.
--- Найдено 16 ссылок. Начинаю загрузку в 'Lingokids_-_Play_and_Learn_us_gplay'...
    [+] screen_1.png (478 KB)
    ...
--- Готово. Скачано файлов: 16
```

## ⚙️ Как это работает

### App Store
1. **iTunes API:** Обращение к `itunes.apple.com/lookup`. Если успешно — ссылки для iPhone, iPad, Apple TV.
2. **Веб-парсинг (Fallback):** Если API пуст — парсинг HTML страницы App Store. Извлечение ссылок из srcset и JSON-LD с фильтрацией мусора.
3. **Хак качества:** Подстановка размера `1284x2778bb.jpg` в URL Apple CDN. Для URL без суффикса (из JSON-LD) суффикс добавляется.
4. **Дедупликация:** По базовому пути изображения (без суффикса размера).

### Google Play
1. **Поиск:** Через `google-play-scraper` — по названию или package name.
2. **Fallback для appId=None:** Если библиотека не вернула ID, парсинг HTML страницы поиска Google Play.
3. **Хак качества:** Суффикс `=w0` для получения оригинального размера (вместо превью).

## 📂 Структура проекта
```
.
├── main.py             # Основной скрипт
├── requirements.txt    # Зависимости
├── .gitignore          # Правила игнорирования файлов
└── README.md           # Документация
```

## 📝 Лицензия
Проект распространяется под лицензией MIT.
