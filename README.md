# App Store Screenshot Downloader

Мощный Python-инструмент для автоматического скачивания скриншотов из Apple App Store в максимальном качестве.

Скрипт использует **гибридный подход**: сначала запрашивает данные через официальный iTunes API (для скорости), а если API возвращает неполные данные (как бывает с приложениями вроде *Lingokids* или *Duolingo*), автоматически переключается на веб-парсинг страницы.

## 🚀 Основные возможности

* **Гибридная загрузка:** Использует API для скорости и парсинг HTML как надежный запасной вариант (Fallback).
* **Максимальное качество:** Автоматически превращает превью-ссылки (thumbnail) в ссылки на оригиналы высокого разрешения (Retina), заменяя, например, `460x...` на `1284x2778`.
* **Строгий порядок:** Сохраняет скриншоты в той же последовательности, как они отображаются в магазине (сначала iPhone, затем iPad), не перемешивая их.
* **Поддержка регионов:** Позволяет скачивать ассеты для любой страны (US, RU, KZ, GB и т.д.).
* **Умная организация:** Создает папки с указанием локали (например, `Telegram_ru`, `Telegram_us`) для удобного сравнения ASO.
* **Фильтрация мусора:** Игнорирует иконки, фавиконки и битые файлы.

## 📋 Требования

* Python 3.6+
* Библиотека `requests`

## 🛠 Установка

1. **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/ВАШ_НИКНЕЙМ/app-store-downloader.git](https://github.com/ВАШ_НИКНЕЙМ/app-store-downloader.git)
    cd app-store-downloader
    ```

2. **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

## 💻 Использование

Запустите скрипт через терминал:

```bash
python main.py
```r

**Интерактивный режим:**
1. Введите ID или название приложения:
- Пример: Telegram или 686449807
2. Введите код страны (по умолчанию us):
- Пример: ru (Россия), us (США), de (Германия).

**Пример работы:**
Plaintext
=== App Store Downloader v7.0 (Strict Order) ===

1. Введите ID или название: Lingokids
2. Страна (us, ru, kz) [us]: us

Цель: Lingokids_-_Play_and_Learn (US) -> Папка: Lingokids_-_Play_and_Learn_us
   [i] API пуст. Перехожу к сканированию сайта...
--- Найдено 28 ссылок. Начинаю загрузку...
    [+] Lingokids_-_Play_and_Learn_us/screen_1.jpg (450 KB)
    [+] Lingokids_-_Play_and_Learn_us/screen_2.jpg (512 KB)
    ...
--- Готово. Скачано файлов: 28

## 📂 Структура проекта
Plaintext
.
├── main.py             # Основной скрипт
├── requirements.txt    # Зависимости
├── .gitignore          # Правила игнорирования файлов
└── README.md           # Документация

## ⚙️ Как это работает
1. Проверка API: Скрипт обращается к itunes.apple.com/lookup. Если успешно — собирает ссылки для iPhone, iPad и Apple TV.
2. Веб-парсинг (Fallback): Если API вернул пустой список (частая проблема для некоторых категорий), скрипт скачивает HTML-страницу приложения в App Store.
3. Хак качества: Скрипт модифицирует URL картинки Apple, подставляя размеры 1284x2778bb или 1024x0w, заставляя сервер отдать изображение наилучшего качества.
4. Дедупликация: Удаляет дубликаты ссылок, строго сохраняя визуальный порядок отображения.

## 📝 Лицензия
Проект распространяется под лицензией MIT.